# Copiar para .env.local (dev) e configurar na Vercel (produção).
#
# Só a anon key entra aqui — ela é pública por design e a proteção vem da RLS.
# A SERVICE_ROLE_KEY e a OPENCODE_API_KEY NÃO pertencem a este arquivo nem à
# Vercel: elas vivem só em worker/.env, na VM.

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
